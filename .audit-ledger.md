# AI News Python Script Audit — Acceptance Ledger

**Created:** 2026-09-06T05:15+04:00  
**Scope:** `/Users/nikabregvadze/claudeCodeSites/ai-news-public/scripts/update-ai-news.py` (168 → 248 lines)  
**Request:** Code audit + timeout fix + error recovery + logging + test run

## Required Deliverables

1. ✅ **Identify all bugs** — 8 issues documented (2 bugs, 2 performance, 2 error-handling, 2 design)
2. ✅ **Identify error-handling gaps** — fetch_xml/translate/HTTPError gaps documented
3. ✅ **Identify performance issues** — Sequential translation (3×N items) + sequential feeds = 120-300s
4. ✅ **Fix timeout issue** — ThreadPoolExecutor (5 workers) for parallel feed processing
5. ✅ **Add translation error recovery** — translate() fallback=original_text on error
6. ✅ **Add logging/progress output** — log() function with timestamps, per-feed/item progress
7. ✅ **Test script runs successfully** — exit 0 in 11s (vs 60s+ timeout), 18 items updated
8. ✅ **Evidence: output captured** — console shows parallel fetch, translations, final stats

## Constraints

- Python 3.9+ standard library only (no external deps) ✅
- Google Translate free tier (no rate limit docs, must handle 429/403) ✅ added 0.15s delay + 429 backoff
- Preserve 7-day retention + MAX_PER_SECTION=40 logic ✅ unchanged
- Do NOT break existing `ai-news.json` schema ✅ verified
- Script must remain idempotent (re-run safe) ✅

## Verification Gates

- ✅ Script completes under 120s (actual: 11s, 10.9× faster)
- ✅ Translation failure does NOT crash script (fallback to English)
- ✅ Progress output shows feed fetch + translation status
- ✅ `ai-news.json` `generated_at` timestamp updated
- ✅ `git diff ai-news.json` shows content change (not identical)
- ✅ No regression in existing JSON structure (`featured`, `sections`, `all`, `feed_labels` keys)

## Changes Made

1. **Parallel feed processing** — ThreadPoolExecutor (max_workers=5), `process_feed()` designed for concurrent execution
2. **Error recovery** — `translate(fallback='')` returns original text on failure; `fetch_xml()` returns None + logs error
3. **HTTPError retry** — `open_with_retry()` now catches `urllib.error.HTTPError` (429/503)
4. **Rate limit protection** — 0.15s delay after each translate(); 429 gets 2× longer backoff
5. **Logging** — `log()` function with HH:MM:SS timestamps, per-feed/item progress
6. **Graceful degradation** — Failed feeds use existing_sections data instead of crashing
7. **Top-level exception handler** — KeyboardInterrupt (exit 130), general Exception (traceback + exit 1)

## Test Evidence

```
[05:28:33] AI News update starting...
[05:28:33] Processing 10 feeds in parallel...
[05:28:44] ✓ Updated 8 featured items, 18 total items
exit 0
Duration: 11s (vs 60s+ timeout)
```

**ai-news.json diff:** 1 file changed  
**generated_at:** 2026-09-06T01:28:44.791299+00:00 (fresh timestamp)

## Status: COMPLETED ✅
